<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>Document</title>
    <style>
         body {
            margin: 0;
        }

        .menu {
            height: 40px;
            width: 100%;
            background-color: red;
            color: white;
            justify-content: space-evenly;
            display: flex;
            align-items: center;
        }

        .menu a {
            width: 210px;
            text-align: center;
            text-decoration: none;
            color: black;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }
    </style>
</head>
<body>
    <span class="menu">
        <a href="/">Список товаров</a>
        <a href="/add">Добавить товар</a>
        <a href="/pickup">Забрать товар</a>
        <a href="/movement">Движение товара</a>
    </span>

    <h3>Поставка товара</h3>
    <table class="table table-striped-columns">
        <tr><th>Дата</th><th>ID транзакции</th><th>Штрих-код товара</th></tr>
        <% for (var i = 0; i < supply.length; i++) {%>
            <tr><td><%=supply[i].date%></td><td><%=supply[i]._id%></td><td><%=supply[i].bar_good%></td></tr>
        <%} %>
    </table>
    <h3>Отгрузка товара</h3>
    <table class="table table-striped-columns">
        <tr><th>Дата</th><th>ID транзакции</th><th>Штрих-код товара</th></tr>
        <% for (var i = 0; i < shipment.length; i++) {%>
            <tr><td><%=shipment[i].date%></td><td><%=shipment[i]._id%></td><td><%=shipment[i].bar_good%></td></tr>
        <%} %>
    </table>
</body>
</html>